PORT=3000
DATABASE_URL=postgres://app_user:app_user_pw@localhost:5432/cleanlife
JWT_SECRET=change_this_to_a_long_random_string
ADMIN_API_KEY=change_this_to_a_long_random_admin_key
REDIS_URL=redis://localhost:6379
# Dispatch timing — defaults match the SRS (2 min admin hold, 5 min cascade step).
# Override with smaller values only for local testing.
ADMIN_HOLD_MS=120000
TIER_CASCADE_STEP_MS=300000
MOMO_WEBHOOK_SECRET=change_this_to_a_long_random_webhook_secret
CORS_ORIGIN=http://localhost:5173
